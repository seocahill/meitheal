<%= form_with model: [:admin, page], class: "space-y-6" do |form| %>
  <% if page.errors.any? %>
    <div class="form-error">
      <h2><%= pluralize(page.errors.count, "error") %> prohibited this page from being saved:</h2>
      <ul class="list-disc list-inside mt-2">
        <% page.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, class: "form-label" %>
    <%= form.text_field :title, class: "form-input" %>
  </div>

  <div>
    <%= form.label :slug, class: "form-label" %>
    <%= form.text_field :slug, class: "form-input", placeholder: "e.g. about-us" %>
    <p class="form-hint">URL path for the page (lowercase letters, numbers, and hyphens only)</p>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
    <div>
      <%= form.label :locale, "Language", class: "form-label" %>
      <%= form.select :locale, [
        ["English (en)", "en"],
        ["Gaeilge (ga)", "ga"]
      ], {}, class: "form-input" %>
      <p class="form-hint">Language of this page version</p>
    </div>

    <div>
      <%= form.label :visibility, class: "form-label" %>
      <%= form.select :visibility, [
        ["Draft (only admins)", :draft],
        ["Published (everyone)", :published],
        ["Members Only", :members_only]
      ], {}, class: "form-input" %>
      <p class="form-hint">Who can view this page</p>
    </div>

    <div>
      <%= form.label :nav_location, "Navigation", class: "form-label" %>
      <%= form.select :nav_location, [
        ["Hidden (no link)", :hidden],
        ["Main Nav", :nav],
        ["Footer", :footer],
        ["Dropdown Menu", :dropdown]
      ], {}, class: "form-input" %>
      <p class="form-hint">Where to show a link to this page</p>
    </div>
  </div>

  <div>
    <%= form.label :content, class: "form-label mb-2" %>
    <%= form.rich_text_area :content %>
  </div>

  <div class="flex items-center justify-end gap-4 pt-4 border-t border-gray-100">
    <%= link_to "Cancel", admin_pages_path, class: "text-gray-600 hover:text-gray-900" %>
    <%= form.submit class: "btn-primary text-white px-6 py-2.5 rounded-xl font-medium cursor-pointer" %>
  </div>
<% end %>
