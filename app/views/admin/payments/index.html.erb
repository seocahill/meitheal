<div class="max-w-6xl mx-auto px-4 py-8">
  <div class="flex justify-between items-center mb-8">
    <h1 class="text-3xl font-bold">Manage Payments</h1>
  </div>

  <!-- Filter tabs -->
  <div class="flex flex-wrap gap-2 mb-6">
    <%= link_to "All", admin_payments_path, class: "px-4 py-2 rounded-lg text-sm font-medium #{params[:payment_method].blank? ? 'bg-purple-100 text-purple-700' : 'text-gray-600 hover:bg-gray-100'}" %>
    <%= link_to "Cash", admin_payments_path(payment_method: "cash"), class: "px-4 py-2 rounded-lg text-sm font-medium #{params[:payment_method] == 'cash' ? 'bg-green-100 text-green-700' : 'text-gray-600 hover:bg-gray-100'}" %>
    <%= link_to "Bank Transfer", admin_payments_path(payment_method: "bank_transfer"), class: "px-4 py-2 rounded-lg text-sm font-medium #{params[:payment_method] == 'bank_transfer' ? 'bg-blue-100 text-blue-700' : 'text-gray-600 hover:bg-gray-100'}" %>
    <%= link_to "SumUp", admin_payments_path(payment_method: "sumup"), class: "px-4 py-2 rounded-lg text-sm font-medium #{params[:payment_method] == 'sumup' ? 'bg-purple-100 text-purple-700' : 'text-gray-600 hover:bg-gray-100'}" %>
    <%= link_to "Other", admin_payments_path(payment_method: "other"), class: "px-4 py-2 rounded-lg text-sm font-medium #{params[:payment_method] == 'other' ? 'bg-gray-200 text-gray-700' : 'text-gray-600 hover:bg-gray-100'}" %>
  </div>

  <!-- Search and date filters -->
  <%= form_with url: admin_payments_path, method: :get, class: "mb-6 flex gap-4 flex-wrap" do |form| %>
    <%= form.text_field :search, value: params[:search], placeholder: "Search user or description...", class: "form-input flex-1 min-w-[200px]" %>
    <%= form.date_field :start_date, value: params[:start_date], placeholder: "From", class: "form-input" %>
    <%= form.date_field :end_date, value: params[:end_date], placeholder: "To", class: "form-input" %>
    <%= form.submit "Filter", class: "bg-pink-600 text-white px-4 py-2 rounded hover:bg-pink-700" %>
    <% if params[:search].present? || params[:start_date].present? %>
      <%= link_to "Clear", admin_payments_path, class: "bg-gray-200 text-gray-700 px-4 py-2 rounded hover:bg-gray-300" %>
    <% end %>
  <% end %>

  <div class="bg-white shadow rounded-lg overflow-x-auto">
    <table class="min-w-full divide-y divide-gray-200">
      <thead class="bg-gray-50">
        <tr>
          <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Date</th>
          <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">User</th>
          <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Purpose</th>
          <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Description</th>
          <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Amount</th>
          <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Method</th>
          <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Status</th>
          <th class="px-4 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
        </tr>
      </thead>
      <tbody class="bg-white divide-y divide-gray-200">
        <% if @payments.any? %>
          <% @payments.each do |payment| %>
            <tr>
              <td class="px-4 py-3 whitespace-nowrap text-sm text-gray-900">
                <%= payment.paid_on.strftime("%b %d, %Y") %>
              </td>
              <td class="px-4 py-3 text-sm">
                <div class="font-medium text-gray-900"><%= payment.user_name %></div>
                <div class="text-gray-500 text-xs"><%= payment.user_email %></div>
              </td>
              <td class="px-4 py-3 whitespace-nowrap">
                <span class="inline-flex px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-800">
                  <%= payment.purpose&.humanize&.sub("Other purpose", "Other") %>
                </span>
              </td>
              <td class="px-4 py-3 text-sm text-gray-900">
                <%= truncate(payment.description, length: 60) %>
              </td>
              <td class="px-4 py-3 whitespace-nowrap text-sm font-medium text-gray-900">
                &euro;<%= number_with_precision(payment.amount_euro, precision: 2) %>
              </td>
              <td class="px-4 py-3 whitespace-nowrap">
                <% badge_class = case payment.payment_method
                   when 'cash' then 'bg-green-100 text-green-800'
                   when 'bank_transfer' then 'bg-blue-100 text-blue-800'
                   when 'sumup' then 'bg-purple-100 text-purple-800'
                   else 'bg-gray-100 text-gray-800'
                   end %>
                <span class="inline-flex px-2.5 py-0.5 rounded-full text-xs font-medium <%= badge_class %>">
                  <%= payment.payment_method.humanize %>
                </span>
              </td>
              <td class="px-4 py-3 whitespace-nowrap">
                <% status_class = case payment.status
                   when 'completed' then 'bg-green-100 text-green-800'
                   when 'pending' then 'bg-yellow-100 text-yellow-800'
                   when 'failed' then 'bg-red-100 text-red-800'
                   end %>
                <span class="inline-flex px-2.5 py-0.5 rounded-full text-xs font-medium <%= status_class %>">
                  <%= payment.status.humanize %>
                </span>
              </td>
              <td class="px-4 py-3 whitespace-nowrap text-right text-sm">
                <%= link_to "View Member", admin_membership_path(payment.membership), class: "px-3 py-1.5 rounded text-xs font-medium text-white bg-purple-600 hover:bg-purple-700 transition-colors" %>
              </td>
            </tr>
          <% end %>
        <% else %>
          <tr>
            <td colspan="8" class="px-6 py-12 text-center text-gray-500">
              No payments found matching the selected filters.
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <!-- Pagination -->
  <nav class="mt-8 text-center text-sm">
    <%== @pagy.series_nav %>
  </nav>
</div>
