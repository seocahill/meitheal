test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

# S3 storage for production
amazon:
  service: S3
  access_key_id: <%= Rails.application.credentials.dig(:aws_access_key_id) || ENV["AWS_ACCESS_KEY_ID"] %>
  secret_access_key: <%= Rails.application.credentials.dig(:aws_secret_access_key) || ENV["AWS_SECRET_ACCESS_KEY"] %>
  region: eu-west-1
  bucket: meitheal-assets-production

# Remember not to checkin your GCS keyfile to a repository
# google:
#   service: GCS
#   project: your_project
#   credentials: <%= Rails.root.join("path/to/gcs.keyfile") %>
#   bucket: your_own_bucket-<%= Rails.env %>

# mirror:
#   service: Mirror
#   primary: local
#   mirrors: [ amazon, google, microsoft ]
